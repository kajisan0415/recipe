
    <%= form_with(model: tweet, local: true) do |form| %>
      <% if tweet.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

          <ul>
          <% tweet.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= form.label :title %>
        <%= form.text_field :title,class:"tweet-form-title" %>
      </div>

      <div class="field">
        <%= form.label :content %>
        <%= form.text_area :content,class:"tweet-form-content" %>
      </div>

      <div class="field">
        <%= form.label :image_id %>
        <%= form.attachment_field :image %>
      </div>

      <div class="actions">
        <%= form.submit %>
      </div>
    <% end %>
